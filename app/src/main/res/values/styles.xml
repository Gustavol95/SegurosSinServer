<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="colorControlNormal">#000000</item>
        <item name="colorControlActivated">#ffffff</item>
        <item name="colorControlHighlight">#ffffff</item>

    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" >
        <item name="colorPrimary">@color/toolbarSeguros</item>
        <item name="colorPrimaryDark">@color/toolbarSeguros</item>
        <item name="colorAccent">@color/toolbarSeguros</item>
    </style>



    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light">
        <item name="colorPrimary">@color/toolbarSeguros</item>
        <item name="colorPrimaryDark">@color/toolbarSeguros</item>
        <item name="colorAccent">@color/toolbarSeguros</item>
    </style>

    <style name="MyAlertDialogStyle" parent="Theme.AppCompat.Light.Dialog.Alert">
        <!-- Used for the buttons -->
        <item name="colorAccent">@android:color/white</item>
        <!-- Used for the title and text -->
        <item name="android:textColorPrimary">@android:color/white</item>
        <!-- Used for the background -->
        <item name="android:background">@color/rectanguloSplash</item>
        <item name="android:divider">@drawable/fondo_seguros</item>
    </style>

    <style name="MyAlertDialogStyleBlanco" parent="Theme.AppCompat.Light.Dialog.Alert">
        <item name="colorAccent">@color/rectanguloSplash</item>
    </style>



</resources>
